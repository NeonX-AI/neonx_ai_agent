services:
  ubuntu-ssh:
    build: .
    container_name: neonx-ubuntu
    ports:
      - "2222:22"
    restart: unless-stopped
    networks:
      - neonx_network

networks:
  neonx_network:
    external: true