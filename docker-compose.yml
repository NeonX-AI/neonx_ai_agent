services:
  ubuntu-ssh:
    build: .
    container_name: neonx-ubuntu
    ports:
      - "2222:22"
    restart: unless-stopped
    networks:
          - neonx-network

    networks:
      neonx-network:
        name: neonx-network
        external: true